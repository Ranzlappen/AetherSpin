"""Custom book events for NovaForged (real math-sdk API).

The SDK emits the standard events (reveal/winInfo/setWin/freeSpin*/finalWin) from
`src.events.events`; this module holds only NovaForged-specific custom events as
the port grows (e.g. expanding-wild and ladder events). Empty for now, matching
`0_0_lines/game_events.py`.
"""
