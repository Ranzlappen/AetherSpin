# AetherSpin math engine dependencies.
#
# The STANDALONE simulator (math/simulator) runs on the Python standard library
# alone — no third-party packages required — so CI math validation is fast and
# hermetic. The optional packages below power analysis, the official-SDK bridge,
# and richer reporting.

# Numerical / data analysis (optional: PAR sheets, distribution analysis)
numpy>=1.26
pandas>=2.1

# Testing & quality
pytest>=9.1.1
pytest-cov>=5.0
ruff>=0.6

# TOML parsing for run_config.toml on Python <3.11 (3.11+ ships tomllib)
tomli>=2.0; python_version < "3.11"

# NOTE: the official StakeEngine/math-sdk has its own requirements and a Rust
# toolchain for the optimizer. Run `bash scripts/setup-math.sh` to fetch it.
